name: INSACERMO CSP-Minizinc-Obj-2016 Blind Planner

on:
  pull_request:
    paths:
      - 'experiments/INSACERMO_CSPMINIZINCOBJ2016_BLIND_PLANNER_PREREG_V1.json'
      - 'experiments/run_cspminizincobj2016_blind_planner_v1.py'
      - '.github/workflows/insacermo_cspminizincobj2016_blind_planner.yml'

jobs:
  execute-frozen-protocol:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install analysis dependencies
        run: python -m pip install --disable-pip-version-check pandas numpy
      - name: Verify preregistration exists before execution
        run: |
          test -f experiments/INSACERMO_CSPMINIZINCOBJ2016_BLIND_PLANNER_PREREG_V1.json
          python - <<'PY'
          import json
          p='experiments/INSACERMO_CSPMINIZINCOBJ2016_BLIND_PLANNER_PREREG_V1.json'
          d=json.load(open(p))
          assert d['status']=='FROZEN_BEFORE_ALGORITHM_RUNS_OPENED'
          print('Frozen preregistration:', d['name'])
          PY
      - name: Execute frozen real-data planner
        run: python experiments/run_cspminizincobj2016_blind_planner_v1.py
      - name: Show machine verdict
        run: cat experiments/cspminizincobj2016_blind_planner_v1_output/REPORT.md
      - name: Upload complete audit artifact
        uses: actions/upload-artifact@v4
        with:
          name: INSACERMO_CSPMINIZINCOBJ2016_BLIND_PLANNER_V1
          path: experiments/cspminizincobj2016_blind_planner_v1_output/
          if-no-files-found: error
